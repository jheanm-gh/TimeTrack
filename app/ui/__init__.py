"""PySide6 user interface.

One compact window, a tray icon, and the dialogs the brief calls for. The
widgets hold no billing logic of their own: every figure they display comes
from :mod:`app.core.calc`, so what is on screen and what is in the workbook
can never disagree.
"""
